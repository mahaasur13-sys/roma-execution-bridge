# =============================================================================
# ROMA — Priority Classes
# =============================================================================

apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: roma-critical
value: 1000000
globalDefault: false
description: "Critical ROMA pods — api-server, gpu-worker, postgres — never evicted"

---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: roma-system
value: 100000
globalDefault: false
description: "Standard ROMA system pods"