apiVersion: v2
name: roma-vault
description: HashiCorp Vault for ROMA secrets management
type: application
version: 0.1.0
appVersion: "1.16.0"
kubeVersion: ">=1.24.0"
dependencies:
  - name: vault
    version: "0.27.0"
    repository: "https://helm.releases.hashicorp.com"
