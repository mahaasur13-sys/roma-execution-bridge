# Итоговый отчёт батча: TRUTH-IN-GATES → R6-LOKI-PERSISTENT (исправленный)

Дата: 2026-09-21 · instance_id: `9a6ceb45-c3e8-4a15-9dfe-35c6d6b9e4ee` · fencing_token: 5 (литеральный `deploy/ops/lease.py status` — в отчёте сессии-исполнителя)
Ветка: `p1/pg-supervision-and-startup` · на remote ветки нет · push/merge/PR не выполнялись

Цепочка коммитов батча:

```
0980470 A-0 protocol: refresh session marker on every heartbeat (detector can now fire)
5c675ee A-1 (N7a): семантика области измерения — glob вместо матча только корня
b20a49c A-2 (N7b): область ПРОГОНА repo-wide + junitxml как машинный источник чисел
1b36014 A-3 (N7c/N7d): политика исключений покрывает xfail; strict=True + issue + expiry
5fd0b34 A-4 (N6/N7a): scope_history — два перехода области измерения
879b6aa A-5: гейт печатает три области одним блоком
fde5094 P-LEASE-3: capture отказывает при живой чужой аренде — проверка TTL до любой записи
74ade31 A-6: полнота набора — collected == executed и точный канон вместо порога "collected >= 244"
b4e3f8a A-7: храповик полов по машинному факту — floor 24.4 -> 31.7, 48.1 -> 48.9
c4a27f4 A-3b: политика skip/skipif — тройка reason+issue+expiry
0c28d85 R5-A: Tenant до Auth — снят обход аутентификации (P1-A)
b26f1d1 R5b: route_job вызывает гейт по фактическому контракту
818b739 R6: журнал pg_watchdog снова доходит до Loki и защищён дрейф-проверкой
```

## 1. Числа единственного прогона

Источник — вывод `ci/coverage_gate.sh` (exit 0) на HEAD `818b739` плюс настоящий документационный коммит
(коммит не трогает код и тесты, поэтому числа тождественны; постоверификационный прогон — в отчёте сессии).

```
RATCHET        : OK · floors 31.7/48.9 >= исторического максимума 31.7/48.9 (записей: 2)
COLLECTED      : 287   (объявлено pytest в junitxml)
EXECUTED       : 287   (элементов <testcase> в том же файле)
OUTCOMES       : 281 passed · 0 failed · 0 errors · 6 skipped = 287
RUN COMPLETENESS: PASSED (collected == executed == канон)
TOTAL          : 32.16% (5007/15567)   floor 31.7    [scope: product-only]
TOTAL ref      : 43.53% (8363/19212)                   [scope: whole-repo, справочно]
MONEY PATH     : 48.94% (485/991)      floor 48.9
COVERAGE GATE  : PASSED · pytest exit: 0
```

Дополнительно из того же контура:

| Проверка | Значение | Марка |
|---|---|---|
| `drift_check.py` | PASSED · pairs=2 compared=2 checks=2 problems=0 | [A] |
| promtail-джоба | CHECK promtail-pg-watchdog: OK sha256=3cdb4bb765b0, джоба `pg_watchdog_persistent` (label `job: pg_watchdog`) на месте | [E] |
| боевой ledger | 309 строк, служебных `test-%` — 0 | [A] |
| Grafana-барьер | probe admin/admin → 401 | [E] |

## 2. История канона набора и причины каждой дельты

| Канон | Дельта | Причина | Прецедент |
|---|---|---|---|
| 254 | 244 → 254 | батч TRUTH-IN-GATES (A-0…A-5): корневой «тест» заменён настоящими проверками, добавлены негативы протокола аренды | — |
| 263 | +9 | P-LEASE-3: негативы протокола аренды (живая чужая аренда, битый heartbeat, два capture — один успех) | — |
| 270 | +7 | A-6: негативы полноты набора (`tests/test_run_completeness.py`) | — |
| 276 | +6 | A-7: негативы храповика полов (`tests/test_coverage_ratchet.py`) | вынужденно, тем же коммитом, запись в истории |
| 280 | +4 | A-3b: проверки политики skip (`tests/test_exclusion_policy.py`, рантайм-бюджет скипов в `tests/conftest.py`) | вынужденно, тем же коммитом |
| 281 | +1 | R5-gateway-auth: снят xfail P1-A (skipped → passing) и добавлен негатив «двойник публичного пути → 401» | перераскладка +1 |
| 287 | +6 | R6-LOKI-PERSISTENT: негативы promtail-дрейфа (`tests/test_promtail_drift_check.py`) | вынужденно, тем же коммитом |

История зафиксирована машинно в `.ci/run-completeness.json`; каждая запись содержит collected/passed/skipped
и текстовую причину. Переиспользование идентификаторов не допускалось: A-6 — полнота набора, A-7 — полы,
A-8/N8 (нормализованный digest) не начат.

## 3. Артефакты прогонов и их SHA-256

| Прогон | Артефакт | SHA-256 | Марка |
|---|---|---|---|
| 270-канон (A-6) | `artifacts/rpt18/junit_final.xml` | `f622fde0f10dfa9f270a3dc5f4057a1c7df6be2b465a97e3fe6225071c200229` | [V] побайтово сверено аудитором |
| A-7 | `artifacts/rpt19/junit_final.xml` | `30d85be63bce38b7cc680c79066c297b261795d4cf0594407c1c29cfb3401745` | [A] |
| A-3b | `artifacts/rpt20/junit_final.xml` | `dac212599bf0cc492227c70d1f9b068c593dd1469eb18d8c4b4ebc9b9564b767` | [A] |
| R5-gateway-auth | `/tmp/roma_junit.xml` + лог прогона | `00c6f0b437187d21f16986ab6c38ae8c173398df1f8e543983775f90ac5b6c5c` | [A] |
| R5b-ROUTING | там же | `c793c0aa07beda52cb656403d1c390482f00b8d1307aedcd3bf430ff4ee2c0e3` | [A] |
| R6-LOKI | там же | `9bb33a844f6ea08c8953cb084b32bdb60f7a796d6c6ffcad5aca857519ec99d0` | [A] |
| суженный набор (негатив A-6) | `artifacts/rpt18/narrowed_junit.xml` | `6d8f285b…` — заявленный узлом, побайтово не воспроизведён при равной длине файла | [S], хэш недостоверен |

Правила доказательности, действующие с этого батча:

1. `sha256` считается после закрытия файла и сверяется с напечатанным гейтом.
2. Доставленное инлайн и сверенное проверяющим — `[V]`; существующее только на узле — `[S]`.
3. Строка `eda382ef…` из пакета RPT-17 **исторически недостоверна** как хэш артефакта: она напечатана гейтом
   для промежуточного файла прогона, а архивный `artifacts/rpt17/junit_final.xml` после закрытия имеет
   `ffd083fb3469ed7b8dc802b433b5b8e0696bcf7e42ff1da892650bbbfbb0aaf3`. Задним числом ничего не переписывалось;
   строка не должна цитироваться как хэш артефакта.

## 4. Критерии приёмки батча и как каждый закрыт

| Критерий | Коммит/прогон | Как закрыт |
|---|---|---|
| 1. Все числа — из машинного источника (junitxml), не из прогресс-строки | `b20a49c` (A-2), `879b6aa` (A-5) | гейт печатает три области одним блоком: область покрытия, область прогона, числитель/знаменатель, источник порогов (файл + sha256) |
| 2. Детектор коллизии сессий способен сработать (негатив A-0) | `0980470`, `tests/test_lease_protocol.py` | два свежих маркера → отказ; `fresh session markers: 1` в каждом отчёте |
| 3. Область измерения: glob-семантика, вложенные тесты больше не занижают TOTAL | `5c675ee` (A-1), `5fd0b34` (A-4) | `scope_history` с двумя переходами (15703 → 15607 → 15347) |
| 4. Политика xfail: `strict=True` + `issue` + `expiry`, позитивное доказательство | `1b36014` (A-3) | канонический JUnit 270 нёс два xfail с `issue:` и `expiry:` (P1-A, P1-B); после R5/R5b живых xfail нет (`skipped` 7 → 6), а политика остаётся исполнимой |
| 5. Политика skip/skipif: тройка reason + issue + expiry, полный инвентарь | `c4a27f4` (A-3b) | расширен сканер `tests/test_exclusion_policy.py`; рантайм-бюджет скипов в `tests/conftest.py`; 6 видимых в каноне скипов легитимированы, негативы сработали до фикса |
| 6. Точная полнота набора: `collected == executed == канон`, негатив сужения выше старого порога | `74ade31` (A-6) | `scripts/ci_run_completeness.py` + `.ci/run-completeness.json`; негатив — реально суженный прогон 246 ≥ 244, который старый порог пропускал |
| 7. Храповик полов по машинному факту | `b4e3f8a` (A-7) | полы 24.4/48.1 → 31.7/48.9, `floor_history`, механическая проверка «пол только вверх» (понижение → exit 3) |
| 8. Блокеры приёмки после батча | `0c28d85`, `b26f1d1`, `818b739` | R5-gateway-auth (порядок middleware, FREE-тир подтверждён экспериментом), R5b-ROUTING (контракт `evaluate(tenant_id, payload)`), R6-LOKI-PERSISTENT (джоба возвращена, снапшот и фингерпринт в git) |

Незакрытые критерии батча отсутствуют; критерии 4 и 5 закрыты политикой исключений, а не только рапортом:
каждое правило имеет сработавший негативный тест.

## 5. Устранение расхождений ревизии №27

### 5.1 Шесть негативов дрейф-проверки (`tests/test_promtail_drift_check.py`)

| # | Тест | Исход |
|---|---|---|
| 1 | `test_pre_fix_config_without_job_is_caught` | passed |
| 2 | `test_fingerprint_drift_is_caught` | passed |
| 3 | `test_label_substitution_is_caught` | passed |
| 4 | `test_live_config_matching_fingerprint_and_snapshot_passes` | passed |
| 5 | `test_node_live_config_matches_committed_snapshot` | passed |
| 6 | `test_repo_snapshot_and_fingerprint_agree` | passed |

Исходы — из `<testcase>` канонического JUnit текущего прогона; в отчёте R6 была показана только часть строк,
здесь список полный.

### 5.2 Имена снапшота promtail: «имя → период → где упоминалось»

| Имя | Период | Где упоминалось |
|---|---|---|
| `pre-r3fix` | до 2026-09-21 (розыск R-6, разведка ревизии №18) | упоминание в разведке; сейчас живёт только как историческое описание в `scripts/drift_check.py` и `tests/test_promtail_drift_check.py`. Имя лгало: файл всегда содержал состояние **с** фиксом |
| `promtail-config.r3fix-applied.yaml` | до R6 (путь `artifacts/a1_evidence/`) | фактический путь снапшота в промежуточном транскрипте; в R6 переименован |
| `promtail-config.with-pg-watchdog-persistent.yaml` | с R6 по настоящее | честное имя; sha256 `3cdb4bb765b02717e8912bedac888612e9a6526de1c3a537585e93e2da618447` — **не менялся** ни при переименованиях |

Строка 116 в `artifacts/WRITER_BATCH_2_REPORT_2026-09-21.md` помечена как исправленная: снапшот всегда содержал
фикс. Канон версии — `deploy/monitoring/promtail-config.yaml` (версионируемый), живой платформенный конфиг
сверяется с ним фингерпринтом и обязательной джобой.

#### Приложение 5.2-A — физическая цепочка имён (LEDGER-FIX, 2026-09-22)

**Пометка к строке 2 таблицы выше (`promtail-config.r3fix-applied.yaml`): строка ошибочна.** Файла с таким
именем на ноде не существовало ни в один момент; следов (inode, mtime, упоминания в артефактах) не найдено ни
одного, кроме самой таблицы. Прежний текст не затирается — пометка внесена новой строкой.

Аттестованная независимой перепроверкой физическая цепочка:

| Этап | Факт | Источник свидетельства |
|---|---|---|
| 1 | `artifacts/a1_evidence/promtail-config.pre-r3fix.yaml` | `artifacts/WRITER_BATCH_2_REPORT_2026-09-21.md:116` (создан до R6) и промежуточный транскрипт 2026-09-21 11:01Z |
| 2 | переименован в `promtail-config.with-pg-watchdog-persistent.yaml` в 2026-09-21 **11:25:50Z** | mtime каталога `artifacts/a1_evidence` |
| 3 | коммит R6 `818b739` — 2026-09-21 **11:29:56Z** (зазор от переименования ≈ 4 мин) | `git log` |

sha256 содержимого `3cdb4bb765b02717e8912bedac888612e9a6526de1c3a537585e93e2da618447` не менялся ни на одном шаге;
байты идентичны в снапшоте, каноне `deploy/monitoring/promtail-config.yaml`, фингерпринт-файле и живом
платформенном конфиге — подтверждено независимым инстансом `[V]`.

## 6. Отложенные решения владельца

| ID | Суть | Требует |
|---|---|---|
| P1-C-кандидат | гейт не консультируется в локальной ветви `route_job` (вне GPU-ветви) | решение владельца |
| fail-open `disabled` | семантика введена `b26f1d1`, покрыта тестом; выбор fail-open/fail-closed | решение владельца |
| P0-SECRETS / SEC-1 / T5-VAST | литеральные значения в `deploy/k8s/secrets/roma-secrets.yaml`, секреты в env сервисов | ротация значений владельцем |
| ledger D4-A | 278–429 служебных `test-*` строк — в боевом ledger на текущий момент 0 (`test-%` = 0, всего 309) | решение владельца о `ledger_prod`/пространстве `t-%` |
| OWNER_RECHECK_POST_BATCH | независимая перепроверка третьим инстансом (автор себя не подтверждает) | вне сессии исполнителя |
| №3-COMPLETE | подтверждение владельца | вне сессии |
| N-BLACK | `black --check` по затронутым файлам, снятие `|| true` с ruff-шага CI, Ruff F841 в `scheduler/roma_scheduler.py` (предсуществующий) | отдельный ГО |
| Подъём пола | факт продукта 32.1642% (5007/15567) при поле 31.7; храповик допускает подъём | отдельный ГО |
| PITR / T5-VAST / T6 | архивация WAL, retention, restore-drill; ротация секретов | отдельные ГО |

## 7. Что не выполнялось

`git push` · ветки/теги/PR на remote · merge/rebase · правка `lease.status` вручную · изменение полов и
`floor_history` · ротация секретов · правки боевых строк ledger · установка пакетов · форматирование (N-BLACK).
