# ROMA + AstroFin — Handoff 2026-08-15

## Состояние сервисов (все UP)

| Сервис | Порт | Supervisor | Примечание |
|--------|------|-----------|------------|
| ROMA API | 8900 | roma-execution-bridge RUNNING | PostgreSQL, pool min=2 max=20, keepalive, GPU unavailable |
| AstroFin API | 8000 | astrofin-api RUNNING | v0.4.0 |
| Grafana | 3000 | astrofin-grafana RUNNING | 13.1.1, дашборды (5 panels), алерты (PostgreSQLHighDeadTuples), Telegram contact point |
| PostgreSQL | 5432 | — | roma DB, 9 таблиц, keepalive active |
| Backup Daemon | — | backup-daemon RUNNING | ежедневно 02:00 UTC, ротация 7 дней |
| Evolution Worker | — | evolution-worker RUNNING | DECAY_KILL_THRESHOLD=-2.0, gen=7, 10-мин цикл |

### Supervisor config
Файл: `/etc/zo/supervisord-user.conf`
- roma-execution-bridge: `PG_DSN=postgresql://postgres:postgres@localhost:5432/roma`
- evolution-worker: `DECAY_KILL_THRESHOLD="-2.0"`
- backup-daemon: `command=bash /home/workspace/roma-execution-bridge/backup_daemon.sh`

## Что сделано в этой сессии (2026-08-15)

### P0 — PostgreSQL
- Миграция SQLite → PostgreSQL: `db_adapter.py` + `db_pg_sync.py` (psycopg2 sync), `db_pg.py` (asyncpg)
- ThreadedConnectionPool: minconn=2, maxconn=20, keepalive (idle=60, interval=10, count=3, connect_timeout=10)
- `main.py`: `import db_adapter as db`, shutdown hook `close_pg_pool()`
- PG_DSN в `/etc/zo/supervisord-user.conf`
- Backup: `backup.sh` + `backup_daemon.sh` (daily 02:00 UTC, 7-day rotation, 4 дампа)
- Smoke-тест: 9/10 PASS

### P1 — Стоимость и планирование
- `cost/config.py` — CostConfig (env-overridable), 11 хардкодов → конфиг
- `cost/gate.py` — TIER_LIMITS + DEFAULT_LIMIT из CostConfig
- `cost/predictor.py` — GPUPolicyEngineV2 интеграция (fallback gpu-node-1)
- Аудит scheduler/cost-gate закрыт

### P1 — Evolution worker
- Kill floor: -0.5 → -2.0 (`DECAY_KILL_THRESHOLD=-2.0` в supervisor)
- Анализ reward: 22 сэмпла, mean=-3.11, trend +0.73, 49 forced resets → снижены
- Работает стабильно, 10-мин цикл

### P1 — GPU monitoring (DCGM)
- Манифест: `deploy/manifests/dcgm-exporter.yaml` (DaemonSet + Service + ServiceMonitor)
- Скрипт: `deploy/scripts/deploy_dcgm.sh` (106 строк)
- GPU-статус в `/health`: `gpu_available: false` (ждём железо)
- Документация: `docs/DCGM-deployment.md`

### P1 — Billing (CloudPayments)
- Скрипт: `deploy/scripts/activate_cloudpayments.sh` (133 строки)
- Документация: `docs/CloudPayments-setup.md`
- Webhook: подпись + идемпотентность, test-режим (CLOUDPAYMENTS_MODE=test)
- Блокировано: ключи не получены

### Grafana
- Дашборд: ROMA PostgreSQL (5 panels)
- Алерты: PostgreSQLHighDeadTuples (2 шт.), исключены _timescaledb_*
- Telegram contact point: `telegram-alerts` (uid=efv8j3dn3bi80c), notification policy active

### Документация
| Файл | Назначение |
|------|-----------|
| `docs/AIOPS-STATUS-2026-08-15.md` | Сводный статус-репорт |
| `docs/production-monitoring.md` | План мониторинга (229 строк) |
| `docs/DCGM-deployment.md` | Инструкция развёртывания GPU-мониторинга |
| `docs/CloudPayments-setup.md` | Инструкция активации биллинга |
| `docs/release-checklist.md` | Чек-лист для продакшен-релиза |
| `docs/AUDIT-SCHEDULER-COST-2026-08-15.md` | Аудит scheduler/cost-gate |

## Ключевые файлы (изменённые)

| Файл | Что |
|------|-----|
| `db_adapter.py` | ThreadedConnectionPool, keepalive, close_pg_pool |
| `db_pg_sync.py` | Sync PostgreSQL adapter (psycopg2) — создан |
| `cost/config.py` | CostConfig dataclass — создан |
| `cost/gate.py` | TIER_LIMITS + DEFAULT_LIMIT из CostConfig |
| `cost/predictor.py` | GPUPolicyEngineV2 integration, policy_engine param |
| `main.py` | `import db_adapter as db`, GPU в /health, shutdown hook |
| `health_gpu.py` | GPU availability check — создан |
| `/etc/zo/supervisord-user.conf` | PG_DSN для roma, DECAY_KILL_THRESHOLD для evolution, backup-daemon |

## Блокировки

| Задача | Причина | Действие при разблокировке |
|--------|--------|---------------------------|
| DCGM GPU monitoring | Нет GPU-железа в песочнице | `./deploy/scripts/deploy_dcgm.sh` |
| CloudPayments live keys | Ключи не получены | `./deploy/scripts/activate_cloudpayments.sh` |

## Быстрый старт (новый чат)

```bash
# Проверка сервисов
curl -s http://localhost:8900/health | jq .status
curl -s http://localhost:8000/health | jq .status
supervisorctl -c /etc/zo/supervisord-user.conf status | grep -E 'roma|backup|evolution'

# PostgreSQL
PG_DSN='postgresql://postgres:postgres@localhost:5432/roma' python3 -c "
import db_adapter as db
print('is_invoice_processed:', db.is_invoice_processed('test-123'))
print('add_lead:', db.add_lead('test@example.com','TestCo','QA'))
"

# Бэкапы
ls -lt /backup/postgres/ | head -5
/home/workspace/roma-execution-bridge/backup.sh

# Evolution worker
grep 'Complete:.*best_reward' /dev/shm/evolution-worker_err.log | tail -3

# Главные документы
cat /home/workspace/roma-execution-bridge/docs/AIOPS-STATUS-2026-08-15.md
cat /home/workspace/roma-execution-bridge/docs/production-monitoring.md
cat /home/workspace/roma-execution-bridge/docs/release-checklist.md
```
