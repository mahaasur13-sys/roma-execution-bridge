# P3.6 — ЧЕКПОЙНТ №1 · G-QUOTA-SOURCE-REMNANTS (подпись значений — до первого коммита)

**Дата:** 2026-09-23 · **Фаза:** P3.6 QUOTA-REMNANTS · **Ветка фазы (ещё не создана):** `p3-6/quota-remnants`
**Аренда:** instance `a9fd8d87-e9b0-47fc-8d23-070ac179afd0`, fencing_token **33**
**HEAD:** `05e78fec54ef83a48222f5cbca68e6df18076bed` (master, = ls-remote master)
**Входная чистота (G-PHANTOM-ENTRY-WRITE):** `git status --porcelain` — пусто; `git diff HEAD --stat` — пусто; HEAD совпадает с ls-remote. Фантома нет.
**Статус:** коммитов не сделано ни одного. Жду подписи владельца на таблицу ниже.

---

## 1. Свип: литеральные источники квот (факты, не карта проекта)

### 1.1 Подписанный источник (эталон)

`config/plans.json` — `jobs_per_month` + `gpu_s_per_job` (месячный ресурс производный, не хранится):

| тир | jobs_per_month | gpu_s_per_job | производное gpu_s_per_month |
|---|---|---|---|
| free | 50 | 3600 | 180 000 |
| pro | 1000 | 3600 | 3 600 000 |
| enterprise | -1 | -1 | -1 (безлимит) |

### 1.2 Остатки (место → поле → сейчас → станет → род живого изменения → досягаемость)

| # | Место | Поле | Сейчас (литерал) | Станет (derive) | Род живого изменения | Досягаемость сегодня |
|---|---|---|---|---|---|---|
| 1 | `deps.py:47-72` `PLANS` | max_jobs_per_month | free 50 · start 50 · pro **150** · ent -1 | 50 / ? / **1000** / -1 | free =, **pro ослабление 6.7×** | **LIVE — принуждается**: `main.py:154 _check_limits` (вызов `main.py:1216 submit_job` → 402). Дисплей: `main.py:1149 /usage`, `main.py:1419` дашборд, `routers/billing.py:244 /balance` |
| 2 | `deps.py:47-72` `PLANS` | max_gpu_seconds | free **300** · start 3600 · pro 36000 · ent -1 | 3600 (per-job) **или** 180 000 (месяц) | **ослабление 12× (per-job) / 600× (месяц)** — величина зависит от семантики поля | **display-only**: единственный читатель `routers/billing.py:245`. Ни один код не сравнивает с ним (public_jobs пишет факт из длительности) |
| 3 | `deps.py:47-72` `PLANS` | spend_cap_usd / overage_rate | 0.50 / 0.0 · 5.00 / 5e-6 · 50.00 / 3e-6 · -1.0 / 0.0 | **в источнике полей НЕТ** | нет derive — вопрос схемы (см. §4.2) | LIVE: `routers/billing.py:159,189,232` (spend-cap/balance), `main.py:79 _check_spend_cap` — **0 вызовов = мёртвая функция** |
| 4 | `tier_profiles.py:8-45` `DEFAULT_TIERS` + `tiers.json` (tracked) | max_jobs_month / max_gpu_seconds_month / budget_limit | start 50/36000/100 · pro **150**/**180000**/500 · ent -1/-1/-1 | jobs 50/?/1000 · gpu per-job или месяц | **pro: 150→1000 (6.7×), 180000→3.6M (20×)**; **start: 36000→3600 (10× сжатие)** | **LIVE**: `policy_engine.py:43-45,76,85` ← `routers/decisions.py`, `routers/jobs.py` ← `main.py:543-544`. `tiers.json` читается приоритетно (`tier_profiles.py:48-55`) |
| 5 | `plugins/builtin/decision_gate.py:45-49` `TIER_LIMITS` | max_jobs / max_gpu_hours / max_concurrent | free **3/2.0h** · pro **20/500h** · ent **500/10000h** | — (таблица не читается) | нет живого изменения (мёртвая таблица) | **DEAD**: нет `manifest.json` в `plugins/builtin` (loader ищет только их), не в `PLUGIN_REGISTRY`, `DecisionGatePlugin` нигде не инстанцируется, `PluginRuntime.load_from_class` вызывается только из demo-`main()` и теста |
| 6 | `billing/cloudpayments_client.py:192` `PLANS` | jobs_per_month / gpu_hours | free 50/0h · pro **500/20h** · ent **999999/200h** | — (таблица не читается) | нет живого изменения | **DEAD**: 0 импортов таблицы (import-и только `CloudPaymentsConfig/Client`) |
| 7 | `main.py:313` `CLOUDPAYMENTS_PLANS` | amount/currency/description | pro «500 задач/мес» · ent «безлимит» | текст из источника | **текстовая правда расходится** (источник: pro 1000) | LIVE: `routers/billing.py:102,118` — цены (не квота); квота только в строке описания |
| 8 | `models/decision.py:135`, `models/__init__.py:49` | max_gpu_seconds_month default | 36000 | ? или fail-closed | неявный дефолт 36000 при отсутствии строки квоты | LIVE default: `cost/enterprise_gate.py:141-144` (`TenantQuota(tenant_id=...)` без строки) |
| 9 | `cost/config.py:11-35` `TIER_LIMITS`/`BENCHMARKS` | money-лимиты + бенчмарк-длительности | FREE 1.0/PRO 50/ENT 500; default 3600 s | — | нет живого изменения | **DEAD**: 0 импортов модуля |
| 10 | `tier_gate.py:4,14` | reader tier_profiles | — | — | нет живого изменения | **DEAD**: 0 импортов модуля |
| 11 | `config/usage.json` (tracked в git) | total_jobs / total_gpu_seconds | tenant-demo 50 · tenant-alpha 1 · demo 16 | — | нет живого изменения | **ORPHAN**: `grep -rn "usage.json"` по коду → **0 читателей** (только `.gitignore:19`, REPORT_LEDGER, `docs/*`) |

### 1.3 Читатели `deps.PLANS` (полный список, продукт без тестов)

| Строка | Что читает | Живой путь? |
|---|---|---|
| `main.py:79` | spend_cap_usd | **нет — 0 вызовов `_check_spend_cap(`** |
| `main.py:155` | max_jobs_per_month (default 50) | да — `POST /submit` → 402 при исчерпании |
| `main.py:1149` | max_jobs_per_month | да — `GET /usage` |
| `main.py:1419` | max_jobs_per_month | да — HTML-дашборд |
| `main.py:1652` | `plan_name = t["plan"] if t else PLANS.get("free", {})` | **found-fact: при отсутствующем tenant в `plan_name` попадает dict, не строка** |
| `routers/billing.py:159,189,232` | spend_cap_usd | да — `/ledger`, `/spend-cap`, `/balance` |
| `routers/billing.py:244,245` | max_jobs_per_month, max_gpu_seconds | да — `/balance` limits (дисплей) |

---

## 2. Матрица расхождений «кто что говорит» по тирам

| тир | plans.json (подписан) | deps.PLANS | tier_profiles/tiers.json | decision_gate (dead) | cloudpayments (dead) |
|---|---|---|---|---|---|
| free | 50 jobs · 3600 s/job | 50 · 300 s · cap $0.50 | — (нет free) | 3 · 2h | 50 · 0h |
| **start** | **ОТСУТСТВУЕТ** | 50 · 3600 · cap $5.00 | 50 · 36000 · budget $100 | — | — |
| pro | **1000** · 3600 s/job | **150** · 36000 · cap $50 | **150** · **180000** · budget $500 | **20** · 500h | **500** · 20h |
| enterprise | -1 · -1 | -1 · -1 · cap -1 | -1 · -1 · budget -1 | **500** · 10000h | **999999** · 200h |

**`start` — первый класс продукта, отсутствующий в источнике:** `db_adapter.py:1585,1625` (дефолт tier `start` при регистрации), `cost/enterprise_gate.py:190` (спец-правило для `start`), `crypto_payments/models.py:66` (START), `policy_engine.py:43` (дефолт `start`), `deps.PLANS`, `tier_profiles`/`tiers.json`. При этом `plan_source.plan_limits("start")` сегодня бросит `PlanSourceError` — **derive для `start` невозможен без решения владельца.**

**Живая дивергенция на принуждаемой оси (RED-якорь для C1, уточнение предпосылки промта):** ось `max_gpu_seconds` нигде не принуждается (display-only), а принуждается `jobs_per_month`: pro-tenant получает **402 на 151-й задаче** через `POST /submit` (`deps` 150) и одновременно **APPROVED до 1000-й** через scheduler/CLI (`plans.json`). Это и есть сегодняшняя «одна задача — два вердикта», но по джобам, а не по 300 секундам.

---

## 3. Предложение C1–C3 (в границах заявленного объёма)

**C1 — API-путь (`deps`/`main`/`routers/billing`)**
`deps.PLANS` заменяется на derive через `plan_source.plan_limits`; `max_jobs_per_month` → `jobs_per_month`, `max_gpu_seconds` → `gpu_s_per_job` (семантику поля выбрать по §4.1). `spend_cap_usd`/`overage_rate` — money-лимиты, не квоты (§4.2).
RED: pro-tenant `POST /submit` 402 на 151-й против scheduler-APPROVED до 1000-й (печать двух ответов). GREEN: один ответ всюду.

**C2 — `plugins/builtin/decision_gate.py`** — доказанно мёртв (нет manifest, нет регистрации, нет инстанцирования): предложение — удалить `TIER_LIMITS` (или модуль целиком) с этими доказательствами в сообщении коммита. Требует подписи.

**C3 — орфан `config/usage.json`** — 0 читателей; удалить. Нюанс: файл **в git** и одновременно в `.gitignore:19` (правки .gitignore — не в объёме, отметить честно).

---

## 4. Три вопроса подписи (без ответа — коммиты не начинаются)

**4.1 Семантика `max_gpu_seconds`.** (а) per-job → free 300→3600 (12× шире, как в тексте промта P3.6); (б) месячный ресурс → free 300→180 000 (600× шире). Предлагаю **(а) per-job + переименование вывода в `/balance`** (`gpu_s_per_job`), чтобы имя поля говорило правду.

**4.2 Money-лимиты (`spend_cap_usd`, `overage_rate`)** — полей в подписанном источнике нет. Варианты: (i) оставить в `deps` как явный money-policy-литерал с комментарием «не квота, источник не покрывает»; (ii) добавить поля в `config/plans.json` отдельным подписанным микро-шагом (значения-то уже подписаны, но это правка источника). Предлагаю **(i)** — эра не трогает источник.

**4.3 `start`-тир.** Варианты: (i) добавить `start` в `config/plans.json` (jobs 50, gpu_s_per_job 3600 — равным `deps`-литералу; note: `tier_profiles` говорит 36 000, расхождение 10×); (ii) маппинг `start → free`; (iii) fail-closed deny (сломает регистрационный дефолт). Предлагаю **(i)** с вашей подписью значения (50/3600).

**4.4 Вне заявленного объёма, но живое (нашлось свипом):** `tier_profiles.py`/`tiers.json` — квоты (`max_jobs_month`, `max_gpu_seconds_month`, `budget_limit`) на LIVE-пути `/decisions` и `/jobs` через `policy_engine`. Промт заявлял четыре источника; фактически их шесть живых. Варианты: (i) включить в C1 (правка станет шире заявленного объёма), (ii) зарегистрировать как **G-QUOTA-SOURCE-REMNANTS-2** и закрыть следующей мини-эпохой. Предлагаю **(ii)** — чтобы не мешать подписанные назначения.

---

## 5. Что не тронуто (обещание эпохи)

Полы 36.3 / 62.2 и `floor_history` — ни байта. `money 750/1111`, канон `384` — неизменны. Значения подписанного `config/plans.json` не меняются (кроме возможного добавления `start` по §4.3 — с отдельной подписью). `test_quota_gate_total.py` (ИХТ-канон «всего подано») — не трогается. Не-объём строкой: G-CONFIRM-PASSTHROUGH-SCHED, G-TENANT-FILTER-REQUIRED, G-ZO-DEPLOY-RED, Jev → №43.
